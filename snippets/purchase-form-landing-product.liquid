{% if array != blank %}

    <style>
        .hide {
            display: none; 
        }
    </style>

    {% capture preorder-box-items %}{% endcapture %}

    {%- for __id in array -%}
        {% assign block = section.blocks | where: "id", __id | first %}
        {% if block.type == 'product_type' %}
            {% assign product_type_title = block.settings.heading | strip_newlines | strip | replace: ' ', '' | downcase %}
        {% endif %}
    {%- endfor -%}

    {% comment %} <template id="purchase-form-source-{{ section.id }}-{{ product_type_title }}"> {% endcomment %}
        <div class="step_content" id="{{ section.id }}-{{ product_type_title }}">
            <div class="step_conten_blocks">

                {%- for __id in array -%}
                    {% assign block = section.blocks | where: "id", __id | first %}
                    {% if block.type == 'product' %}
                        {% assign product = all_products[block.settings.product] %}

                        {% if product != blank and product.available %}

                            {% assign product_variant_id = product.variants.first.id %}
                            {% assign product_price_divided = product.price | divided_by: 4 %}
                            {% assign discount_percentage = product.price | times: 1.0 | divided_by: product.compare_at_price | minus: 1 | abs | times: 100 | ceil %}
                            {% assign product_title_tag = block.settings.heading | strip_newlines | strip | replace: ' ', '' | downcase %}
                            {% assign product_price = product.price | money_without_trailing_zeros %}
                            {% assign product_price_compare = product.compare_at_price | money_without_trailing_zeros %}

                            {% if block.settings.heading contains '3 Month' %}
                                {% assign product_type = 1 %}
                                {% assign product_hash = 'month_3' %}
                            {% endif %}

                            {% if block.settings.heading contains '2 Month' %}
                                {% assign product_type = 2 %}
                                {% assign product_hash = 'month_2' %}
                            {% endif %}

                            {% if block.settings.heading contains '1 Month' %}
                                {% assign product_type = 3 %}
                                {% assign product_hash = 'month_1' %}
                            {% endif %}

                            {% capture data-per %}Buy now {% if product_type == 1 or product_type == 2 %} & SAVE {{ discount_percentage }}%{% endif %}{% endcapture %}

                            {% if block.settings.preorder == true %}
                                {% capture data-per %}Pre Order{% endcapture %}
                            {% endif %}

                            {% if block.settings.soldout == true %}
                                {% capture data-per %}Sold Out{% endcapture %}
                            {% endif %}

                            <div class="planBlock" href="javascript:void(0)"
                                data-tab="{{ product_hash }}" 
                                data-pay="{{ product_price_divided | money_without_trailing_zeros }}" 
                                data-per="{{ data-per }}" 
                                data-product_variant_id="{{ product_variant_id }}" 
                                data-soldout="{{ block.settings.soldout }}" 
                                data-preorder="{{ block.settings.preorder }}" 
                                data-type="{{ product_type }}"
                            >
                                {% if product_type == 1 %}
                                    <div class="plan_choosed">
                                        <span>OVER 5,000 PEOPLE CHOSE THIS SUPPLY</span>
                                    </div>                                    
                                {% endif %}

                                <input type="radio" name="mBanner_monthlyPlan" id="{{ section.id }}-{{ product_title_tag }}" {% if block.settings.preselected == true %} checked{% endif %}>
                                <label class="monthly_plans-new monthly_plans" for="{{ section.id }}-{{ product_title_tag }}">
                                    <div class="plan_des">
                                        <div class="plan_hdr">
                                            <h3>{{ block.settings.heading }}</h3>

                                            {% if product_type == 1 or product_type == 2 %}
                                                <span class="saved_price">SAVE {{ discount_percentage }}%</span>
                                            {% endif %}
                                        </div>
                                        <div class="plan_des">{{ block.settings.subheading }}</div>
                                    </div>

                                    {% if product_type == 3 %}
                                        <div class="price">
                                            <div class="plan_prc sale_price">{{ product_price }}</div>
                                        </div>
                                    {% else %}
                                        <div class="price">
                                            <div class="plan_prc regular_price">{{ product_price_compare }}</div>
                                            <div class="plan_prc sale_price">{{ product_price }}</div>
                                        </div>                                        
                                    {% endif %}
                                </label>
                            </div>

                        {% endif %}
                    {% endif %}
                {%- endfor -%}


                {{ preorder-box-items }}
                {{ preorder_box }}                
            </div>
        </div>
    {% comment %} </template> {% endcomment %}
{% endif %}


{% comment %} 
<div class="step_content">
    <div class="step_conten_blocks">
        <div class="planBlock">
            <div class="plan_choosed">
                <span>OVER 5,000 PEOPLE CHOSE THIS SUPPLY</span>
            </div>
            <input type="radio" name="mBanner_monthlyPlan"
                id="mBanner_3_month" checked="">
            <label class="monthly_plans-new monthly_plans"
                for="mBanner_3_month" payment-price="$42.25">
                <div class="plan_des">
                    <div class="plan_hdr">
                        <h3>3 Month Supply</h3>
                        <span class="saved_price">& SAVE 43%</span>
                    </div>
                    <div class="plan_des">6 Micro-Infusion
                        Treatments</div>
                </div>
                <div class="price">
                    <div class="plan_prc regular_price">$297</div>
                    <div class="plan_prc sale_price">$169</div>
                </div>
            </label>
        </div>
        <div class="planBlock">
            <input type="radio" name="mBanner_monthlyPlan"
                id="mBanner_2_month">
            <label class="monthly_plans-new monthly_plans"
                for="mBanner_2_month" payment-price="$34.75">
                <div class="plan_des">
                    <div class="plan_hdr">
                        <h3>2 Month Supply</h3>
                        <span class="saved_price">SAVE 25%</span>
                    </div>
                    <div class="plan_des">4 Micro-Infusion
                        Treatments</div>
                </div>
                <div class="price">
                    <div class="plan_prc regular_price">$198</div>
                    <div class="plan_prc sale_price">$149</div>
                </div>
            </label>
        </div>
        <div class="planBlock">
            <input type="radio" name="mBanner_monthlyPlan"
                id="mBanner_1_month">
            <label class="monthly_plans-new monthly_plans"
                for="mBanner_1_month" payment-price="$24.75">
                <div class="plan_des">
                    <div class="plan_hdr">
                        <h3>1 Month Supply</h3>
                        <span class="saved_price d-none"> -
                            $99</span>
                    </div>
                    <div class="plan_des">2 Micro-Infusion
                        Treatments</div>
                </div>
                <div class="price">
                    <div class="plan_prc sale_price">$99</div>
                </div>
            </label>
        </div>
    </div>
</div>
{% endcomment %}