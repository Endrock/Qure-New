{% comment %} <h1>[{{ section.settings.heading }}]</h1> {% endcomment %}

{%- schema -%}
{
    "name":"t:sections.purchase-form.name",
    "class":"top-purchase-form",
    "settings":[
        {
            "type": "text",
            "id": "heading",
            "label": "t:common.heading.label"
        }
    ],
    "presets":[
        {
            "name":"t:sections.purchase-form.name"
        }
    ]
}
{%- endschema -%}