{{ section.settings.code }}

{% schema %}
    {
        "name": "t:sections.liquid.name",
        "class": "section-liquid",
        "settings": [
            {
                "type": "liquid",
                "id": "code",
                "label": "t:common.code.label"
            },
            {
                "type": "checkbox",
                "id": "popup_micro_infusion_enabled",
                "label": "Popup Micro Infusion Enabled",
                "default": true,
                "info": "this will enable the popup micro infusion only for: qure-microinfusion-offer and qure-micro-infusion-landing-page-affiliate"
            }
        ],
        "presets": [
			{
				"name": "t:sections.liquid.name"
			}
        ]
    }
{% endschema %}